from django.test import TestCase

# Create your tests here.

"""
### EXAMPLES
	Procfile
	web: gunicorn dphone.wsgi

	from django.db.models import Q
	Phones.objects.filter(Q(id__contains=7) & Q(id__contains=1)).all()
"""